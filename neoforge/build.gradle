plugins {
    id 'com.github.johnrengelman.shadow'
}

architectury {
    platformSetupLoomIde()
    neoForge()
}

loom {
    accessWidenerPath = project(":common-api").loom.accessWidenerPath
}

configurations {
    common
    shadowCommon
    compileClasspath.extendsFrom common
    runtimeClasspath.extendsFrom common
    developmentNeoForge.extendsFrom common
}

repositories {
    maven {
        name = 'NeoForged'
        url = 'https://maven.neoforged.net/releases'
    }
}

dependencies {
    neoForge "net.neoforged:neoforge:$rootProject.neoforge_version"

    modImplementation("mezz.jei:jei-$rootProject.minecraft_version-neoforge:${jei_version}")

    implementation "cn.6tail:lunar:1.7.3"
    implementation "de.marhali:json5-java:3.0.0"
    shadowCommon "cn.6tail:lunar:1.7.3"
    shadowCommon "de.marhali:json5-java:3.0.0"
    modLocalRuntime("cn.6tail:lunar:1.7.3")
    modLocalRuntime("de.marhali:json5-java:3.0.0")
    forgeRuntimeLibrary(shadowCommon("cn.6tail:lunar:1.7.3"))
    forgeRuntimeLibrary(shadowCommon("de.marhali:json5-java:3.0.0"))

    common(project(path: ':common-api', configuration: 'namedElements')) { transitive false }
    shadowCommon (project(path: ':common-api', configuration: 'transformProductionNeoForge')) { transitive false }
}

processResources {
    inputs.property 'version', project.version

    filesMatching('META-INF/neoforge.mods.toml') {
        expand version: project.version
    }
}

shadowJar {
    relocate "cn.6tail.lunar", "${rootProject.maven_group}.oelib.shadowed.cn.6tail.lunar"
    relocate "de.marhali.json5", "${rootProject.maven_group}.oelib.shadowed.de.marhali.json5"
    configurations = [project.configurations.shadowCommon]
    archiveClassifier = "dev-shadow"
}


java {
    withSourcesJar()
}

remapJar {
    input.set shadowJar.archiveFile
    dependsOn shadowJar
    archiveClassifier = null
}

publishing {
    publications {
        mavenNeoForge(MavenPublication) {
            artifactId = base.archivesName.get()
            artifact(remapJar) {
                builtBy tasks.remapJar
                classifier null
            }
            artifact(tasks.sourcesJar) {
                classifier "sources"
            }
        }
    }
}
